<div class="modal fade" id="sitemap-error-modal" tabindex="-1" role="dialog" aria-labelledby="sitemap-error-modalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header text-center">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">
            <%= image_tag('close-modal.svg', class: 'close-modal') %>
          </span>
        </button>
        <h4>You can't proceed <%= image_tag('sad.png', class: 'sad') %> </h4>
        <p>You are currently on Starter plan which allows only 3 sitemaps.</p>
        <p>Want to add more sitemaps? <br>
        <% if request.original_fullpath == settings_users_path %>
          <%= link_to 'Switch to Pro plan', "#choose-plan-modal", 'data-toggle': "modal", 'data-dismiss': 'modal' %>
        <% else %>
          <%= link_to 'Switch to Pro plan', billing_settings_users_path %>
        <% end %>
         instead. </p>
      </div>
    </div>
  </div>
</div>

